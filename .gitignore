.env
.env.*
!.env.example
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
.benchmarks/
.codegraph/
.serena/
.tokensave/
node_modules/
.opencode/
.claude/
.codex/
.agents/skills/agenticloop/
.agents/plugins/marketplace.json
.cursor/
.github/agents/
.github/skills/agenticloop/
.github/prompts/agenticloop.prompt.md
plugins/agenticloop/
# Generated host adapter output for the toolkit itself.
# Target projects generate their own adapter output under their chosen
# output directory; for toolkit verification, generate under .agenticloop/tmp/
# or keep the legacy root tmp/ path only for compatibility.
opencode.jsonc
.agenticloop/tmp/
# Per-lane parallel worktrees live inside the repo root to stay within the
# host's workspace sandbox; never report them as untracked.
.agenticloop/worktrees/
.agenticloop/local/
.agenticloop/host-role-capabilities/
tmp/
!memory/scaffold/tmp/
!memory/scaffold/tmp/.gitkeep
*.log
*.jsonl
.DS_Store
Thumbs.db
.vscode/
.idea/

# Internal working docs are local-only and never tracked.
/.docs
.mcp.json
# Playwright MCP session snapshots are host runtime output, never repo source.
.playwright-mcp/
